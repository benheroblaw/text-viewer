Scripts are written as plain text, with the extension .scri.
Each line will be displayed as its own dialogue, requiring the user to continue.
Adding a hashtag or two slashes will comment the line, causing it to not be displayed.
Next- extra strings.